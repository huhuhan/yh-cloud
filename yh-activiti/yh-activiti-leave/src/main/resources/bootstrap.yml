eureka:
  client:
    serviceUrl:
      defaultZone: http://yh:yh@yh-eureka:8761/eureka/
  instance:
    instance-id: ${spring.application.name}:${spring.cloud.client.ip-address}:${server.port}
    #服务实例主页的相对地址
    status-page-url-path: /swagger-ui.html
